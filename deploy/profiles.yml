vc_dbt:
  target: motherduck
  outputs:
    motherduck:
      type: duckdb
      path: "md:vc_pipeline"
      motherduck_token: "{{ env_var('MOTHERDUCK_TOKEN') }}"
      token: "{{ env_var('MOTHERDUCK_TOKEN') }}"
